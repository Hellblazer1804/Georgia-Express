CREATE TABLE IF NOT EXISTS card(
    cardNumber INTEGER,
    ExpirationDate DATE,
    cvv INTEGER,
    creditLimit INTEGER,
    cardBalance INTEGER,
    minimumPayment INTEGER,
    rewardPoints INTEGER,
    customer_id INTEGER
);