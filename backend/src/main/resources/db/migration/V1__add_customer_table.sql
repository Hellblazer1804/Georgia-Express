CREATE TABLE IF NOT EXISTS customer(
    customerId INTEGER,
    name VARCHAR(255),
    ssn VARCHAR(10),
    creditScore INTEGER,
    dob DATE,
    address VARCHAR(255),
    email VARCHAR(255),
    phoneNumber VARCHAR(11),
    salary INTEGER
);